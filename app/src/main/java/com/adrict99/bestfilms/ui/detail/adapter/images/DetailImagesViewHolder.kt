package com.adrict99.bestfilms.ui.detail.adapter.images

import android.widget.ImageView
import androidx.recyclerview.widget.RecyclerView
import com.adrict99.bestfilms.databinding.ImageItemBinding

class DetailImagesViewHolder(private val binding: ImageItemBinding) : RecyclerView.ViewHolder(binding.root) {
    fun bind(
        item: ImageView,
        onItemClick: (ImageView) -> Unit
    ) {
        //TODO: Setup image
    }
}